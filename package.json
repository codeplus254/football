{
  "name": "root",
  "private": true,
  "dependencies": {
    "lerna": "^3.15.0"
  },
  "scripts": {
    "start": "lerna run start --parallel",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install:all": "lerna exec \"npm install\" --stream",
    "heroku-postbuild": "lerna bootstrap && lerna run build --parallel",
    "lerna-bootstrap": "lerna bootstrap"
  }
}
